import styled from "@emotion/styled";
import { MarkdownWrapper } from ".";

// NOTE : about markdown wrapper
export const AboutMarkdownWrapper = styled(MarkdownWrapper)`
	li {
		@media (min-width: 1024px) {
			font-size: 1.2rem;
		}
	}
`;
